import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'udemy-ionic5-movies',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
