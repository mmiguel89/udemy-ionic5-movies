import { ArrayPairPipe } from './array-pair.pipe';

describe('ArrayPairPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayPairPipe();
    expect(pipe).toBeTruthy();
  });
});
